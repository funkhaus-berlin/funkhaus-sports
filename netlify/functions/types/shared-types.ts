/**
 * This file re-exports types from the frontend to ensure type consistency
 * across frontend and backend code.
 * 
 * The frontend is considered the source of truth for types.
 */

// Re-export all shared types from the frontend
export * from '../../../src/types'